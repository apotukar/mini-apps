        <li>
            <strong>{{ ev.timestamp | formatWeekday }} {{ ev.timestamp | formatDateTime }}</strong><br>
          {{ ev.description }}<br>
          {% if ev.location %}
            <small>Ort: {{ ev.location }}</small>
          {% endif %}
        </li>
